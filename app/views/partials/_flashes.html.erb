<% flash.each do |key, value| %>
 <div class="alert alert-info trans" id="flash"><%= value %></div>
<% end %>

	<% if current_user %>
	<div class="text-center login-float" id="flash2">
		<h1>Welcome, <%= current_user.name %></h1>
		<h4>Logged in with <strong><em> Github </em></strong></h4>
		<%= image_tag('bestopus.png', width: '200') %>
	</div>
	<% end %>